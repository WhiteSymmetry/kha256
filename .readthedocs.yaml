version: 2

build:
  os: ubuntu-26.04
  tools:
    python: "3.14"  # Use a stable version

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
    - path: .  # Install your package: kha256
